***tags***: #Python #fundamentals 

- `input()` keyword is the most basic way to make python programs interactive
-  It always returns the string
```python 

a = input("Enter your name")
b = int(input("Enter your age"))

```